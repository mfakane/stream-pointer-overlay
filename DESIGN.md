# デザインシステム

## Shibui / Stream Pointer dark

主なタスクは、ポインターを調整し、動きとホットスポットを確認して OBS 用 CSS
を取得すること。GUI
ツールとして、プレビューと操作説明、設定と画像調整をそれぞれ同時に参照できる配置を保つ。CSS
本文はラベル付きの details で開閉し、コピー操作とエラーは常時表示する。OBS
導入手順はユーザー指定により常時表示とし、見出しを左、保存リンクを右に配置し、その下に
3 手順をカラム配置する。横並びのカラム間には 1 px
の縦罫線を置き、縦並びになる境界では非表示にする。コピー失敗時は CSS
本文を自動で開く。

ツールバーは設定画面のチェックボックスで常時表示を切り替え、Custom CSS の
`--toolbar-always-visible` を `0`/`1`
として出力する。配信画面の外に操作領域を置く 使い方として、OBS
ブラウザソースの高さを配信解像度より約 80 px 高くする案内を設定画面と README
に明記する。

基本色は既存のダーク背景 `#101317`、文字 `#edf2f4`、アクセント `#bcf478`
を維持する。背景の役割は canvas、操作領域の region、選択状態の
selected。通常のセクションは余白・整列・見出しで区切り、カード背景、装飾罫線、バッジ、装飾アイコン、ぼかしを使わない。コントロールの角丸は
2
px。入力と選択肢には操作可能な境界を残し、選択肢はチェック記号と太字でも識別する。描画モードは輪郭と
aria-pressed で選択を示す。

機能上の例外を次のとおり定義する。これらは UI の装飾用 surface には数えない。

- プレビューの 32 px
  グリッドは、ユーザー指定により元の表示を維持する。位置や移動量を確認するための目安として使う。
- ホットスポット拡大図の市松模様は画像の透明部分を識別するためのもの。conic-gradient
  をその描画手段として維持する。
- ホットスポットの十字、描画カーソルの中心点の輪郭、スポットライトの暗幕は任意の画像・配信背景上で位置や照明範囲を示す。既存の
  box-shadow による描画を維持する。
- 円形の波紋、ブラシ、スポットライト、色見本は対象の形や描画色を表す。描画色と位置マーカーの意味色は
  UI アクセントとは別に維持する。

## レビュー

- IA-01〜04: 設定・確認・CSS
  取得が主タスク。補足はラベル付き開閉。調整用画像は必要な編集コンテキストとして常設。
- ST-01〜06: 汎用カードと通常セクションの罫線を除去。設定と画像の間は 48
  px。ユーザー指定により、その中央に 1 px
  の縦罫線を置き、編集と画像確認の境界を示す。線は画像領域の高さに追従し上下 8
  px を空ける。760 px
  以下では縦に配置して罫線を非表示にする。作業領域の既存最大幅を維持。
- VI-01〜06、CO-01〜06:
  上記の機能例外以外に装飾的な影・グラデーションを使わない。状態はテキスト、入力境界とラベルを維持。リセットは共通
  ghost ボタン。
- AC-01〜04: button/link/input/textarea/summary
  のフォーカスを表示。透明なラジオは親ラベルにフォーカスを表示。選択は色以外でも識別。reduced-motion
  ではツールバーとカーソルの遷移を止め、波紋は拡大せずフェードのみ (既存の
  animationend による除去を維持)。

## 検証結果

Deno 型チェック・既存テスト 2 件・単一 HTML ビルド成功。Edge で 1280 px、390
px、320 px 幅を確認し、横はみ出しがないこと、選択状態の更新、CSS と OBS
手順の開閉、キーボードでの開閉とフォーカス表示を確認。OBS
実機での再確認はユーザーにより実施。

## 日本語の表記

[Microsoft Japanese Style Guide](https://download.microsoft.com/download/a/8/2/a822a118-18d4-4429-b857-1b65ab388315/jpn-jpn-StyleGuide.pdf)
を参考に、画面文言・アクセシビリティ用ラベル・説明文・プロジェクト文書で次の表記を統一する。

- 日本語と半角英数字の間に半角スペースを入れる。例:
  `OBS で使う`、`待機時間 0 で無効`。
- 丸括弧は半角を使い、外側の本文との間に半角スペースを入れる。内側には入れない。例:
  `対話 (Interact)`、`カスタム (画像未選択)`。句読点や閉じかぎ括弧の直前、文末には不要なスペースを入れない。
- 説明文の数値と単位を分ける。例:
  `1 px`。日本語の句読点・かぎ括弧・中黒の前後にはスペースを追加しない。
- コード、CSS 値、識別子、URL、ファイルパス、SVG
  データには文章向けの変換を適用しない。
